<?php
namespace Admin\Backend\Common;

class Model {
  // protected $host = $_ENV['DB_HOST'];
  // protected $dbanme = $_ENV['DB_NAME'];
  // protected $username = $_ENV['DB_USERNAME'];
  // protected $password = $_ENV['DB_PASSWORD'];

  public function __construct() {
    
  }
}